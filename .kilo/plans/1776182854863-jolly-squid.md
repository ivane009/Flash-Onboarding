# Plan: Buy Sats Page

## Files to Create/Modify

### 1. New Files
- `public/html/comprar-sats.html` - Main HTML page
- `public/js/comprar-sats.js` - JavaScript for the page

### 2. Structure

**Header (nav):**
- Dark blue background (same as index: `--bg: #0d1b2a`)
- Left: flash.png + "Flash" text white
- Right: 
  - Language pill button with flag + code (ES)
  - Avatar circle with user initials (dark text on white bg)
  - Click avatar → dropdown menu with: user full name, "Mi espacio" link, "Log out" option

**Main Content:**
- Light blue background with dot pattern
- White card with rounded corners
- Decorative 4-point star in top-right corner of card

**Card Content:**
- Tabs: Buy (active/underlined) | Sell
- Title: "Buy Sats"
- XOF input (light gray bg)
- Swap button (circular with ↕️ icon)
- Sats input (light gray bg)
- Receiving address label + ⓘ icon + Lightning address input + QR button
- Payment label + country selector + phone input
- "Confirm and pay" button (full width, accent color)

### 3. Color Palette (from index)
- `--bg: #0d1b2a` (dark blue - header)
- `--accent: #06b6d4` (cyan - buttons)
- `--surface: #1b2838`
- `--text: #ffffff`
- `--text-muted: #94a3b8`

### 4. Implementation Steps
1. Create HTML structure with nav header and main content
2. Create JS for:
   - User avatar dropdown menu toggle
   - Tab switching (Buy/Sell)
   - Swap button functionality (XOF ↔ Sats)
   - Language selector
3. Add CSS styles matching index design
4. Create "Mi espacio" placeholder HTML for redirect
